// postcss.config.js
import tailwindcss from 'tailwindcss';
import autoprefixer from 'autoprefixer';

export default {
  plugins: {
    'tailwindcss': {}, // Simple config that uses default tailwind config file
    'autoprefixer': {},
  }
}